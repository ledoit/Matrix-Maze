{
  "_comment": "Game name: 'matrix-maze' - if changing, also update src/constants.js, Cargo.toml, tauri.conf.json, and index.html",
  "name": "matrix-maze",
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:wasm": "bash scripts/build-wasm.sh",
    "build:web": "vite build --base=/play/ --outDir ../play --emptyOutDir",
    "preview": "vite preview",
    "tauri": "tauri",
    "music:sync": "node ./scripts/sync-music-from-kaiser.js --project matrix-maze"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "vite": "^4.4.0"
  }
}
